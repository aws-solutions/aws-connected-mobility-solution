diff --git a/source/packages/services/assetlibrary/infrastructure/cfn-neptune.yaml b/source/packages/services/assetlibrary/infrastructure/cfn-neptune.yaml
index 731097b..25ecdc0 100644
--- a/packages/services/assetlibrary/infrastructure/cfn-neptune.yaml
+++ b/packages/services/assetlibrary/infrastructure/cfn-neptune.yaml
@@ -257,7 +257,7 @@ Resources:
   NeptuneDBClusterParameterGroup:
     Type: 'AWS::Neptune::DBClusterParameterGroup'
     Properties:
-      Family: neptune1
+      Family: neptune1.2
       Description: CDF parameters
       Parameters:
         neptune_enable_audit_log: !Ref NeptuneEnableAuditLog
@@ -270,7 +270,7 @@ Resources:
   NeptuneDBParameterGroup:
     Type: 'AWS::Neptune::DBParameterGroup'
     Properties:
-      Family: neptune1
+      Family: neptune1.2
       Description: CDF parameters
       Parameters:
         neptune_query_timeout: !Ref NeptuneQueryTimeout

diff --git a/source/infrastructure/cfn-cdf-core-services-A.yaml b/source/infrastructure/cfn-cdf-core-services-A.yaml
index 996a28c..71f74f5 100644
--- a/infrastructure/cfn-cdf-core-services-A.yaml
+++ b/infrastructure/cfn-cdf-core-services-A.yaml
@@ -270,14 +270,7 @@ Parameters:
   NeptuneDbInstanceType:
     Description: Neptune DB instance type
     Type: String
-    Default: db.r4.xlarge
-    AllowedValues:
-      - db.t3.medium
-      - db.r4.large
-      - db.r4.xlarge
-      - db.r4.2xlarge
-      - db.r4.4xlarge
-      - db.r4.8xlarge
+    Default:  db.r5.large
     ConstraintDescription: Must be a valid Neptune instance type.
 
   NeptuneSnapshotIdentifier:
diff --git a/source/infrastructure/cfn-cdf-core-single-stack.yaml b/source/infrastructure/cfn-cdf-core-single-stack.yaml
index 335e5d6..69be5b4 100644
--- a/infrastructure/cfn-cdf-core-single-stack.yaml
+++ b/infrastructure/cfn-cdf-core-single-stack.yaml
@@ -306,14 +306,7 @@ Parameters:
   NeptuneDbInstanceType:
     Description: Neptune DB instance type
     Type: String
-    Default: db.r4.xlarge
-    AllowedValues:
-      - db.t3.medium
-      - db.r4.large
-      - db.r4.xlarge
-      - db.r4.2xlarge
-      - db.r4.4xlarge
-      - db.r4.8xlarge
+    Default: db.r5.xlarge
     ConstraintDescription: Must be a valid Neptune instance type.
 
 Conditions:
diff --git a/source/infrastructure/deploy-core-single-stack.bash b/source/infrastructure/deploy-core-single-stack.bash
index 3b89415..d461624 100755
--- a/infrastructure/deploy-core-single-stack.bash
+++ b/infrastructure/deploy-core-single-stack.bash
@@ -65,13 +65,7 @@ OPTIONAL ARGUMENTS
     -m (string)   Asset Library mode ('full' or 'lite').  Defaults to full if not provided.
     -p (string)   The name of the key pair to use to deploy the Bastion EC2 host (required for Asset Library (full) mode or Private auth mode).
     -i (string)   The remote access CIDR to configure Bastion SSH access (e.g. 1.2.3.4/32) (required for Asset Library (full) mode).
-    -u (string)   The Neptune DB Instance type. Must be from the following list (default is db.r4.xlarge):
-                  - db.t3.medium
-                  - db.r4.large
-                  - db.r4.xlarge
-                  - db.r4.2xlarge
-                  - db.r4.4xlarge
-                  - db.r4.8xlarge
+    -u (string)   The Neptune DB Instance type. Must be from the following list (default is db.r5.large):
 
     COMPILING OPTIONS:
     ------------------
diff --git a/source/infrastructure/deploy-core.bash b/source/infrastructure/deploy-core.bash
index 6e04fe2..68a6ff8 100755
--- a/infrastructure/deploy-core.bash
+++ b/infrastructure/deploy-core.bash
@@ -79,14 +79,7 @@ OPTIONAL ARGUMENTS
     -m (string)   Asset Library mode ('full' or 'lite').  Defaults to full if not provided.
     -p (string)   The name of the key pair to use to deploy the Bastion EC2 host (required for Asset Library (full) mode or Private auth mode).
     -i (string)   The remote access CIDR to configure Bastion SSH access (e.g. 1.2.3.4/32) (required for Asset Library (full) mode).
-    -u (string)   The Neptune DB Instance type. Must be from the following list (default is db.r4.xlarge):
-                  - db.t3.medium
-                  - db.r4.large
-                  - db.r4.xlarge
-                  - db.r4.2xlarge
-                  - db.r4.4xlarge
-                  - db.r4.8xlarge
-
+    -u (string)   The Neptune DB Instance type. Must be from the following list (default is db.r5.large):
     -x (number)   No. of concurrent executions to provision.
     -s (flag)     Apply autoscaling as defined in ./cfn-autosclaling.yml
 
diff --git a/source/packages/services/assetlibrary/infrastructure/cfn-assetLibrary-parent.yaml b/source/packages/services/assetlibrary/infrastructure/cfn-assetLibrary-parent.yaml
index 2814ddb..4ded8de 100644
--- a/packages/services/assetlibrary/infrastructure/cfn-assetLibrary-parent.yaml
+++ b/packages/services/assetlibrary/infrastructure/cfn-assetLibrary-parent.yaml
@@ -100,14 +100,7 @@ Parameters:
   NeptuneDbInstanceType:
     Description: Neptune DB instance type
     Type: String
-    Default: db.r4.xlarge
-    AllowedValues:
-      - db.t3.medium
-      - db.r4.large
-      - db.r4.xlarge
-      - db.r4.2xlarge
-      - db.r4.4xlarge
-      - db.r4.8xlarge
+    Default: db.r5.large
     ConstraintDescription: Must be a valid Neptune instance type.
 
   CustomResourceVPCLambdaArn:
diff --git a/source/packages/services/assetlibrary/infrastructure/cfn-neptune.yaml b/source/packages/services/assetlibrary/infrastructure/cfn-neptune.yaml
index 731097b..0b1b513 100644
--- a/packages/services/assetlibrary/infrastructure/cfn-neptune.yaml
+++ b/packages/services/assetlibrary/infrastructure/cfn-neptune.yaml
@@ -33,14 +33,7 @@ Parameters:
   DbInstanceType:
     Description: Neptune DB instance type
     Type: String
-    Default: db.r4.xlarge
-    AllowedValues:
-      - db.t3.medium
-      - db.r4.large
-      - db.r4.xlarge
-      - db.r4.2xlarge
-      - db.r4.4xlarge
-      - db.r4.8xlarge
+    Default: db.r5.large
     ConstraintDescription: Must be a valid Neptune instance type.
   CreateDBReplicaInstance:
     Description: Create a read replica for MultiAZ?
diff --git a/source/packages/services/assetlibrary/infrastructure/deploy-cfn.bash b/source/packages/services/assetlibrary/infrastructure/deploy-cfn.bash
index bbcf0aa..33ed889 100755
--- a/packages/services/assetlibrary/infrastructure/deploy-cfn.bash
+++ b/packages/services/assetlibrary/infrastructure/deploy-cfn.bash
@@ -48,13 +48,7 @@ OPTIONAL ARGUMENTS:
                   - LambdaToken
                   - ApiKey
                   - IAM
-    -u (string)   The Neptune DB Instance type. Must be from the following list (default is db.r4.xlarge):
-                  - db.t3.medium
-                  - db.r4.large
-                  - db.r4.xlarge
-                  - db.r4.2xlarge
-                  - db.r4.4xlarge
-                  - db.r4.8xlarge
+    -u (string)   The Neptune DB Instance type. Must be from the following list (default is db.r5.large):
 
     Required if deploying in full mode, or private api auth:
     --------------------------------------------------------
